<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gaebalfan.erp.mapper.OperatingExpenseMapper">

    <resultMap id="ExpenseResultMap" type="com.gaebalfan.erp.domain.OperatingExpense">
        <id     property="expenseId"   column="expense_id"/>
        <result property="expenseType" column="expense_type"/>
        <result property="amount"      column="amount"/>
        <result property="expenseDate" column="expense_date"/>
    </resultMap>

    <select id="findAll" resultMap="ExpenseResultMap">
        SELECT * FROM operating_expenses ORDER BY expense_date DESC, expense_id DESC
    </select>

    <select id="count" resultType="int">
        SELECT COUNT(*) FROM operating_expenses
        <where>
            <if test="q != null and q != ''">
                (expense_type LIKE CONCAT('%',#{q},'%')
                 OR CAST(expense_date AS CHAR) LIKE CONCAT('%',#{q},'%'))
            </if>
        </where>
    </select>

    <select id="findAllPaged" resultMap="ExpenseResultMap">
        SELECT * FROM operating_expenses
        <where>
            <if test="q != null and q != ''">
                (expense_type LIKE CONCAT('%',#{q},'%')
                 OR CAST(expense_date AS CHAR) LIKE CONCAT('%',#{q},'%'))
            </if>
        </where>
        ORDER BY expense_date DESC, expense_id DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <insert id="insert" parameterType="com.gaebalfan.erp.domain.OperatingExpense"
            useGeneratedKeys="true" keyProperty="expenseId">
        INSERT INTO operating_expenses (expense_type, amount, expense_date)
        VALUES (#{expenseType}, #{amount}, #{expenseDate})
    </insert>

    <select id="findMonthlySummary" resultType="map">
        SELECT
            MONTH(expense_date)  AS month,
            SUM(amount)          AS total_expense
        FROM operating_expenses
        WHERE YEAR(expense_date) = #{year}
        GROUP BY MONTH(expense_date)
        ORDER BY MONTH(expense_date)
    </select>

    <select id="findYearlySummary" resultType="map">
        SELECT
            COALESCE(SUM(amount), 0) AS total_expense
        FROM operating_expenses
        WHERE YEAR(expense_date) = #{year}
    </select>

    <select id="findSummaryByType" resultType="map">
        SELECT
            expense_type        AS expense_type,
            SUM(amount)         AS total_amount
        FROM operating_expenses
        WHERE YEAR(expense_date) = #{year}
        GROUP BY expense_type
        ORDER BY SUM(amount) DESC
    </select>

</mapper>
