<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gaebalfan.erp.mapper.SupplierMapper">

    <resultMap id="SupplierResultMap" type="com.gaebalfan.erp.domain.Supplier">
        <id     property="supplierId"   column="supplier_id"/>
        <result property="supplierName" column="supplier_name"/>
        <result property="phone"        column="phone"/>
        <result property="address"      column="address"/>
    </resultMap>

    <select id="findAll" resultMap="SupplierResultMap">
        SELECT * FROM suppliers ORDER BY supplier_id
    </select>

    <select id="findById" resultMap="SupplierResultMap">
        SELECT * FROM suppliers WHERE supplier_id = #{supplierId}
    </select>

    <insert id="insert" parameterType="com.gaebalfan.erp.domain.Supplier"
            useGeneratedKeys="true" keyProperty="supplierId">
        INSERT INTO suppliers (supplier_name, phone, address)
        VALUES (#{supplierName}, #{phone}, #{address})
    </insert>

    <update id="update" parameterType="com.gaebalfan.erp.domain.Supplier">
        UPDATE suppliers
        SET supplier_name = #{supplierName},
            phone         = #{phone},
            address       = #{address}
        WHERE supplier_id = #{supplierId}
    </update>

    <delete id="delete">
        DELETE FROM suppliers WHERE supplier_id = #{supplierId}
    </delete>

</mapper>
