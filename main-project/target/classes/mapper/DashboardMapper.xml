<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gaebalfan.erp.mapper.DashboardMapper">

    <select id="countProducts" resultType="int">
        SELECT COUNT(*) FROM products
    </select>

    <select id="countActiveEmployees" resultType="int">
        SELECT COUNT(*) FROM employees WHERE status = 'ACTIVE'
    </select>

    <select id="countPendingOrders" resultType="int">
        SELECT COUNT(*) FROM purchase_orders WHERE status IN ('PENDING', 'APPROVED')
    </select>

    <select id="countLowInventory" resultType="int">
        SELECT COUNT(*) FROM inventory WHERE quantity &lt;= 10
    </select>

    <select id="countMonthlyWorkOrders" resultType="int">
        SELECT COUNT(*) FROM work_orders
        WHERE status = 'COMPLETED'
          AND YEAR(start_date) = YEAR(NOW())
          AND MONTH(start_date) = MONTH(NOW())
    </select>

    <select id="getMonthlyRevenue" resultType="map">
        SELECT
            COALESCE(SUM(total_price), 0) AS revenue,
            COUNT(*) AS sale_count
        FROM sales
        WHERE YEAR(sale_date) = YEAR(NOW())
          AND MONTH(sale_date) = MONTH(NOW())
    </select>

    <select id="getRecentOrders" resultType="map">
        SELECT po.po_id, po.status, po.order_date, s.supplier_name
        FROM purchase_orders po
        LEFT JOIN suppliers s ON po.supplier_id = s.supplier_id
        ORDER BY po.po_id DESC
        LIMIT 5
    </select>

    <select id="getLowInventoryItems" resultType="map">
        SELECT i.inventory_id, i.quantity, p.product_name, w.warehouse_name
        FROM inventory i
        LEFT JOIN products p ON i.product_id = p.product_id
        LEFT JOIN warehouses w ON i.warehouse_id = w.warehouse_id
        WHERE i.quantity &lt;= 10
        ORDER BY i.quantity ASC
        LIMIT 5
    </select>

    <select id="getWorkOrderStatusCount" resultType="map">
        SELECT status, COUNT(*) AS cnt
        FROM work_orders
        GROUP BY status
    </select>

</mapper>
